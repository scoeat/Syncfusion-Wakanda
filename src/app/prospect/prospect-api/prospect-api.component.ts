import { Component, OnInit, Inject } from '@angular/core';
// import { Observable } from 'rxjs';
// import { DataStateChangeEventArgs } from '@syncfusion/ej2-grids';
// import { GridComponent } from '@syncfusion/ej2-angular-grids';


// import { WakandaRestService } from '../../services/wakanda-rest.service';
// const SERVICE_URI: string = 'http://localhost:4200/rest/';

@Component({
  selector: 'app-prospect-api',
  templateUrl: './prospect-api.component.html',
  styleUrls: ['./prospect-api.component.css']
})
export class ProspectApiComponent {
  // public restdata: Observable<DataStateChangeEventArgs>;
  // public pageOptions: Object;
  // public state: DataStateChangeEventArgs;

//   constructor(@Inject('sourceFiles') private sourceFiles: any, private service: WakandaRestService) {
//     sourceFiles.files = ['wakanda-rest.service.ts'];
//     this.restdata = service;
// }

  // public dataStateChange(state: DataStateChangeEventArgs): void {
      // this.service.execute(state);
  }

  // public ngOnInit(): void {
      // this.pageOptions = { pageSize: 10, pageCount: 4 };
      // let state = { skip: 0, take: 10 };
      // this.service.execute(state);
//   }
// }

